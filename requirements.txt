# Network Mapper Requirements - DevSecOps Enhanced
# Core dependencies for basic functionality
tqdm>=4.64.0          # For progress bars and real-time progress indicators
colorama>=0.4.4       # For colored terminal output

# GUI Dependencies (Required for GUI versions)
tkinter               # GUI framework (included in Python standard library)
requests>=2.31.0      # HTTP requests for vulnerability data and updates

# Network Analysis Dependencies
scapy>=2.5.0          # Advanced packet capture and network analysis
python-nmap>=0.7.1    # Advanced nmap integration
networkx>=3.1         # Network graph analysis and topology mapping
matplotlib>=3.7.0     # Network visualization and charts

# DevSecOps Security Dependencies (Required)
cryptography>=41.0.0  # For encryption, secure storage, and cryptographic operations
keyring>=24.0.0       # For secure credential storage in system keyring
python-dotenv>=1.0.0  # For secure environment variable management

# Security Analysis Tools (Development/CI)
bandit>=1.7.5         # Static Application Security Testing (SAST)
safety>=2.3.0         # Dependency vulnerability scanning
semgrep>=1.45.0       # Advanced static analysis for security

# Testing Dependencies
pytest>=7.4.0        # Unit testing framework
pytest-cov>=4.1.0    # Test coverage reporting

# Code Quality Tools
black>=23.0.0         # Code formatting
flake8>=6.0.0         # Code linting
mypy>=1.5.0           # Static type checking

# PDF Report Generation Dependencies
reportlab>=4.0.0      # Professional PDF generation library
weasyprint>=60.0      # HTML to PDF conversion with CSS support
Pillow>=10.0.0        # Image processing for PDF reports

# Optional Enhanced Features
python-nmap>=0.7.1    # Advanced nmap integration (optional)
tabulate>=0.9.0       # Better table formatting (optional)
matplotlib>=3.7.0     # Network visualization (future enhancement)
networkx>=3.1         # Network graph analysis (future enhancement)

# Database Support (for compliance reporting)
# sqlite3 is included in Python standard library

# System Requirements:
# - Python 3.8+ (recommended 3.9+)
# - Windows/Linux/macOS compatible
# - Administrative privileges may be required for some scanning features

# Security Notes:
# - All dependencies are regularly scanned for vulnerabilities
# - Use 'safety check' to verify dependency security
# - Run 'bandit -r modules/' for security analysis
# - Execute 'semgrep --config=auto .' for advanced security checks